I"�#<h3 id="source-zvonorgcomprtut-regexphtml"><a href="http://zvon.org/comp/r/tut-Regexp.html#Pages~Contents">Source: zvon.org/comp/r/tut-Regexp.html</a></h3>

<p>다방면에서 두루두루 사용되는 정규표현식을 간략하게 정리해 보면,</p>

<blockquote>
  <ol>
    <li>대소문자 구분한다.</li>
    <li>white space 문자를 포함한다 - 스페이스, 탭, 엔터</li>
    <li><code class="language-plaintext highlighter-rouge">^</code> - 맨앞, <code class="language-plaintext highlighter-rouge">$</code>- 맨끝. 그러니까 <code class="language-plaintext highlighter-rouge">^$</code>는 처음부터 끝까지. 전부.</li>
    <li>만약 <code class="language-plaintext highlighter-rouge">^</code>, <code class="language-plaintext highlighter-rouge">$</code>와 같은 특별한 문자를 그대로 표현하고 싶으면, <code class="language-plaintext highlighter-rouge">\</code>를 앞에 붙이자. <code class="language-plaintext highlighter-rouge">\</code>를 표현하고 싶다면? 두 개 쓰면 된다. <code class="language-plaintext highlighter-rouge">\\</code></li>
    <li><code class="language-plaintext highlighter-rouge">.</code>, 그러니까 점은, 어느 문자에나 매칭된다. 점이 여러 개 있다면,</li>
    <li>
      <table>
        <tbody>
          <tr>
            <td><code class="language-plaintext highlighter-rouge">[]</code>는 이 안의 문자와 같은 문자를 지칭한다. 그러니까 <code class="language-plaintext highlighter-rouge">[sdf]</code>면, <code class="language-plaintext highlighter-rouge">s</code></td>
            <td><code class="language-plaintext highlighter-rouge">d</code></td>
            <td><code class="language-plaintext highlighter-rouge">f</code> 인 한 글자를 의미한다.</td>
          </tr>
        </tbody>
      </table>
    </li>
    <li><code class="language-plaintext highlighter-rouge">[ - ]</code>는 문자의 범위를 나타낸다. 여러 개의 범위는 <code class="language-plaintext highlighter-rouge">[C-Ka-d2-6]</code>와 같이 표현한다. 이 식은 세 가지의 범위를 표현한다.</li>
    <li>
      <table>
        <tbody>
          <tr>
            <td><code class="language-plaintext highlighter-rouge">[]</code>안에서 <code class="language-plaintext highlighter-rouge">^</code>는 not의 의미다. <code class="language-plaintext highlighter-rouge">[^sgS]</code>는 그러니까 <code class="language-plaintext highlighter-rouge">s</code></td>
            <td><code class="language-plaintext highlighter-rouge">g</code></td>
            <td><code class="language-plaintext highlighter-rouge">S</code>가 제외된 모든 문자란 표현이다.</td>
          </tr>
        </tbody>
      </table>
    </li>
    <li>
      <table>
        <tbody>
          <tr>
            <td><code class="language-plaintext highlighter-rouge">(|)</code>는 문자열 덩어리를 표현한다. <code class="language-plaintext highlighter-rouge">(snd|s12)</code>는 <code class="language-plaintext highlighter-rouge">snd</code></td>
            <td><code class="language-plaintext highlighter-rouge">s12</code> 가 덩어리로 포함된 표현이다.</td>
          </tr>
        </tbody>
      </table>
    </li>
    <li><code class="language-plaintext highlighter-rouge">*</code>는 없거나 연속, <code class="language-plaintext highlighter-rouge">+</code>는 하나 이상 연속, <code class="language-plaintext highlighter-rouge">?</code>는 없거나 하나만.</li>
    <li>10번에 대한 <code class="language-plaintext highlighter-rouge">?</code>의 예시 - <code class="language-plaintext highlighter-rouge">[^@]@?@</code>: <code class="language-plaintext highlighter-rouge">@</code>가 아닌 문자 하나와, <code class="language-plaintext highlighter-rouge">@</code>인 문자 사이에, <code class="language-plaintext highlighter-rouge">@</code>가 없거나 하나 있는 표현이다.</li>
    <li><code class="language-plaintext highlighter-rouge">{n}</code>는 바로 앞의 문자 한 글자가 정확히 n번 반복인 경우를 표현한다.</li>
    <li><code class="language-plaintext highlighter-rouge">{m,n}</code>는 m번 이상, n번 이하의 반복이다.</li>
    <li>같은 의미를 가지는 표현들: <code class="language-plaintext highlighter-rouge">*</code> ~ <code class="language-plaintext highlighter-rouge">{0,}</code>, <code class="language-plaintext highlighter-rouge">+</code> ~ <code class="language-plaintext highlighter-rouge">{1,}</code>, <code class="language-plaintext highlighter-rouge">?</code> ~ <code class="language-plaintext highlighter-rouge">{0,1}</code></li>
    <li>기본적으로, 표현들은 가능한 가장 많은 매칭을 한다. 하지만 <code class="language-plaintext highlighter-rouge">?</code>가 붙으면, 가장 최소한의 매칭만 찾도록 바뀐다. <code class="language-plaintext highlighter-rouge">r.*?</code>는 그러니까, <code class="language-plaintext highlighter-rouge">r</code> 한 글자와만 매칭된다.</li>
    <li><code class="language-plaintext highlighter-rouge">\w</code>는 알파벳 대소문자, 숫자, 그리고 언더바 <code class="language-plaintext highlighter-rouge">_</code>를 표현한다. 몇몇 언어에서는 문자 약어들은 인식되지 않는다. 그런 경우 그냥 <code class="language-plaintext highlighter-rouge">[A-z0-9_]</code>를 사용한다.</li>
    <li><code class="language-plaintext highlighter-rouge">\W</code>는 알파벳 대소문자, 숫자, 그리고 언더바만 빼고 모두 포현한다. 그러니까 <code class="language-plaintext highlighter-rouge">[^A-z0-9_]</code>이다.</li>
    <li><code class="language-plaintext highlighter-rouge">\s</code>는 white space 문자들을 표현한다 - 스페이스, 엔터, 탭. 반대로 <code class="language-plaintext highlighter-rouge">\S</code>는 그 밖의 모든 문자를 표현한다.</li>
    <li><code class="language-plaintext highlighter-rouge">\d</code>는 모든 숫자를 표현하고, <code class="language-plaintext highlighter-rouge">\D</code>는 그 반대이다. 숫자가 아닌 모든 문자를 표현한다. 시스템에서 지원 안하면, <code class="language-plaintext highlighter-rouge">[0-9]</code>쓰면 된다.</li>
    <li><code class="language-plaintext highlighter-rouge">\b</code>는, 한 문자가 아니라, <code class="language-plaintext highlighter-rouge">\w</code>와 <code class="language-plaintext highlighter-rouge">\W</code> 사이를 표현한다.</li>
    <li><code class="language-plaintext highlighter-rouge">\B</code>는, 그렇다면 당연하게도, <code class="language-plaintext highlighter-rouge">\b</code>가 아닌 간격이다.</li>
    <li><code class="language-plaintext highlighter-rouge">\A</code>와 <code class="language-plaintext highlighter-rouge">\Z</code>는 각각 문자열의 처음과 마지막을 가리킨다. 이는 <code class="language-plaintext highlighter-rouge">^</code>, <code class="language-plaintext highlighter-rouge">$</code>와 비슷하나, 이와는 다르게 매 새로운 라인마다 적용되지 않는다. 그냥 문자열의 생 처음 혹은 생 마지막만 가리킨다.</li>
    <li><code class="language-plaintext highlighter-rouge">(?=&lt;pattern&gt;)</code>는 해당 패턴이 있으면 그 앞을 가리키지만, 해당 패턴 자체를 표현하지는 않는다. 소비되지 않는다.</li>
    <li><code class="language-plaintext highlighter-rouge">(?!&lt;pattern&gt;)</code>는 해당 패턴이 있으면 그 앞을 가리키고, 그리고 그 앞을 표현에서 제외시킨다. 역시 소비되지 않는다.</li>
  </ol>
</blockquote>

<p>여기서부터는 앞의 정규표현 튜토리얼에 없다.</p>

<blockquote>
  <ol>
    <li>24번은 전방 탐색으로 불린다.</li>
    <li><code class="language-plaintext highlighter-rouge">(?&lt;=&lt;pattern&gt;)</code>는 위와는 방향이 반대라고 이해하면 된다. 해당 패턴이 있으면 그 뒤를 가리키고, 자체는 소비하지 않는다. 예시는 다음과 같다</li>
  </ol>
</blockquote>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&gt;&gt;&gt; import re

&gt;&gt;&gt; txt = 'ABC01: $23.45\n\
    HGG42: $5.31\n\
    CFMX1: $899.00\n\
    XTC99: $69.96'

&gt;&gt;&gt; print(txt)
ABC01: $23.45
HGG42: $5.31
CFMX1: $899.00
XTC99: $69.96

&gt;&gt;&gt; re.findall('(?&lt;=\$)[0-9.]+', txt)
['23.45', '5.31', '899.00', '69.96']
</code></pre></div></div>

<p>보면 <code class="language-plaintext highlighter-rouge">(?&lt;=\$)[0-9.]+</code>라는 표현은 <code class="language-plaintext highlighter-rouge">$</code>문자 패턴의 뒤를 보면서, 해당 패턴을 소비하진 않고, 숫자와 <code class="language-plaintext highlighter-rouge">.</code>을 포함한 문자가 하나 이상 반복되는 문자를 표현하고 있다.</p>

<blockquote>
  <ol>
    <li><code class="language-plaintext highlighter-rouge">(?&lt;!&lt;pattern&gt;)</code>은 26과는 반대로, 특정 패턴의 뒤를 보되 찾은 문자열을 표현에서 제외시킨다는 의미이다.</li>
  </ol>
</blockquote>
:ET